global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: distributed-cache-api
    metrics_path: /api/metrics
    static_configs:
      - targets:
          - api-a:3000
          - api-b:3000
